import random
random.seed(1) # força mostrar o mesmo resultado (por posição)
lista = [6, 7, 46]
numero = random.choice(lista) # escolhe um numero aleatorio da lista
numero = random.randint(0, 10)
print(numero) # escolhe um aleatoriamente